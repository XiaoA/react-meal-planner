import React from 'react';

const Navigation = () => {
  return (
    <nav style={ {display: 'flex', justifyContent: 'flex-start'} }>
      <h2 className='fs link dim black pa3 pointer'>Meal Planner</h2>
    </nav>
  );
};

export default Navigation;
